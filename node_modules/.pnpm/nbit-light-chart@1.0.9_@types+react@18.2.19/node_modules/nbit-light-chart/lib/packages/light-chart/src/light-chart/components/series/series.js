'use strict';

var React = require('react');
require('./series.css.js');
var seriesChart = require('./series-chart.js');
var theme = require('../../util/theme.js');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var React__default = /*#__PURE__*/_interopDefaultLegacy(React);

const Series = (props) => {
    const { bgColor, textColor, brandColor } = theme.getTheme();
    const [chartLayoutOptions, setChartLayoutOptions] = React.useState({
        background: {
            color: bgColor,
        },
        textColor,
    });
    React.useEffect(() => {
        const _bgColor = theme.getTheme().bgColor;
        const _textColor = theme.getTheme().textColor;
        setChartLayoutOptions({
            background: {
                color: _bgColor,
            },
            textColor: _textColor,
        });
    }, [props.theme]);
    return (React__default["default"].createElement("div", { className: "series-chart-wrap" },
        React__default["default"].createElement("div", { className: "chart-wrap" },
            React__default["default"].createElement(seriesChart, { chartLayoutOptions: chartLayoutOptions, data: props.seriesData, createChart: { brandColor } }))));
};
var series = React.memo(Series);

module.exports = series;
