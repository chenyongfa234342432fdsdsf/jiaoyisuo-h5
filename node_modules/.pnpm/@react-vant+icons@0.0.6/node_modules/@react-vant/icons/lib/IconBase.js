"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _react = _interopRequireDefault(require("react"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function kebabCase(str) {
  return str.substr(3).replace(/([A-Z])/g, '-$1').toLowerCase().replace(/^-/, '');
}

const IconBase = ({
  name = '',
  className,
  style,
  spin,
  rotate,
  children,
  ...props
}) => {
  const svgStyle = {};

  if (rotate) {
    svgStyle.msTransform = `rotate(${rotate}deg)`;
    svgStyle.transform = `rotate(${rotate}deg)`;
  }

  const kebabCaseName = name ? kebabCase(name) : undefined;
  const attrs = {
    'aria-label': kebabCaseName,
    className: ['rv-icon', kebabCaseName ? `rv-icon-${kebabCaseName}` : '', className, spin ? 'rv-icon--spin' : ''].join(' ').trim(),
    style: { ...style,
      ...svgStyle
    },
    ...props
  };
  return _react.default.cloneElement(children, attrs);
};

var _default = IconBase;
exports.default = _default;