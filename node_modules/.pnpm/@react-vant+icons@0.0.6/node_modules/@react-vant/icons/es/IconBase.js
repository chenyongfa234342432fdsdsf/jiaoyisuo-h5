import React from 'react';
function kebabCase(str) {
    return str
        .substr(3)
        .replace(/([A-Z])/g, '-$1')
        .toLowerCase()
        .replace(/^-/, '');
}
const IconBase = ({ name = '', className, style, spin, rotate, children, ...props }) => {
    const svgStyle = {};
    if (rotate) {
        svgStyle.msTransform = `rotate(${rotate}deg)`;
        svgStyle.transform = `rotate(${rotate}deg)`;
    }
    const kebabCaseName = name ? kebabCase(name) : undefined;
    const attrs = {
        'aria-label': kebabCaseName,
        className: [
            'rv-icon',
            kebabCaseName ? `rv-icon-${kebabCaseName}` : '',
            className,
            spin ? 'rv-icon--spin' : '',
        ]
            .join(' ')
            .trim(),
        style: { ...style, ...svgStyle },
        ...props,
    };
    return React.cloneElement(children, attrs);
};
export default IconBase;
