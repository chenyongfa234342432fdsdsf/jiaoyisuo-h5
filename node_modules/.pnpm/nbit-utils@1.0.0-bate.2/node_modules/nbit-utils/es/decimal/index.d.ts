import Decimal from 'decimal.js';
/**
 * 将安全金额的字符串还原回原始数字字符串，方便和后续的 decimal 计算，一般配合 useSetSafeCurrency 方法使用
 * @param safeCurrency 安全金额的字符串
 * @returns 去除掉其他符号的数字字符串
 */
export declare function getSafeCurrency(safeCurrency: string): string;
/** 将值安全转换为 Decimal 数据 */
export declare function getSafeDecimal(value: string | number): Decimal;
/**
 * 将字符串数字或 NaN 转为安全数字
 * @param value 目标值
 * @param backup 托底值
 */
export declare function useSafeNumber(value: number | string, backup?: number): number;
